# Ambient Email Agent – test harness defaults
#
# Source this file alongside your secrets when running pytest or reminder smoke tests.
# Example:
#   set -a
#   source .env.example   # populates GOOGLE_API_KEY/LANGSMITH_API_KEY/etc.
#   source .env.testing   # applies deterministic test toggles
#   set +a
#
# Secrets are intentionally omitted here; provide them via `.env`, `.env.example`,
# or your shell before sourcing this file.

# Live-first policy: run against real Gemini unless you explicitly flip to offline mode.
EMAIL_ASSISTANT_EVAL_MODE=0
LANGSMITH_TRACING=true
EMAIL_ASSISTANT_TRACE_STAGE=local-test
EMAIL_ASSISTANT_TRACE_TAGS=pytest

# HITL / Gmail tooling defaults so reminder + inbox flows progress without manual approval.
HITL_AUTO_ACCEPT=1
EMAIL_ASSISTANT_SKIP_MARK_AS_READ=1
EMAIL_ASSISTANT_RECIPIENT_IN_EMAIL_ADDRESS=0

# Judge and telemetry toggles for regression runs.
EMAIL_ASSISTANT_LLM_JUDGE=1
EMAIL_ASSISTANT_JUDGE_STRICT=1
EMAIL_ASSISTANT_FEEDBACK_DELAY=0.05

# Reminder worker baseline so node QA can assert consistent timings.
REMINDER_DEFAULT_HOURS=48
REMINDER_POLL_INTERVAL_MIN=15
REMINDER_LABEL_PENDING=Pending
REMINDER_LABEL_DUE=Due
REMINDER_LABEL_DONE=Done
